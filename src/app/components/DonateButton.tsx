'use client';
import React from 'react'

const DonateButton = () => {
  return (
    <div>
      <button onClick={() => console.log("Click")}>Donate</button>
    </div>
  );
}

export default DonateButton
